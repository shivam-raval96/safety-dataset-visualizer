'use client';
import { useMemo, useState } from 'react';
export type Dataset={name:string;org:string;category:string;size:string;year:number;license:string;url:string;paper?:string;desc:string;tags:string[];x:number;y:number;citations:string};
const categories=['All datasets','Jailbreak / red-teaming','Deception','Reward hacking','Agentic','Multiagent','Eval awareness','Bias'];
const colors:Record<string,string>={'Jailbreak / red-teaming':'#ff866a',Deception:'#58d7bf','Reward hacking':'#9b7bff',Agentic:'#78a8ff',Multiagent:'#ed7cbe','Eval awareness':'#f2b84b',Bias:'#3eb6c4'};

function pointSize(sampleSize:string){
  const match=sampleSize.replaceAll(',','').trim().match(/^<?\s*([\d.]+)\s*([kKmM])?/);
  if(!match)return 8;
  const multiplier=match[2]?.toLowerCase()==='m'?1_000_000:match[2]?.toLowerCase()==='k'?1_000:1;
  const samples=Number(match[1])*multiplier;
  return Number((8+18*Math.sqrt(Math.min(samples,50_000)/50_000)).toFixed(2));
}
export default function DatasetAtlas({datasets}:{datasets:Dataset[]}){const[selected,setSelected]=useState(datasets[0]);const[filter,setFilter]=useState('All datasets');const[query,setQuery]=useState('');const visible=useMemo(()=>datasets.filter(d=>(filter==='All datasets'||d.category===filter)&&`${d.name} ${d.org} ${d.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())),[datasets,filter,query]);const visibleLabelNames=useMemo(()=>{const names=new Set(visible.slice().sort((a,b)=>pointSize(b.size)-pointSize(a.size)).slice(0,12).map(d=>d.name));if(visible.some(d=>d.name===selected.name))names.add(selected.name);return names},[visible,selected]);return <main className="app-shell">
<header className="topbar"><div className="brand"><span className="brandmark"><i/><i/><i/></span><span>Dataset Atlas</span><em>beta</em></div><div className="search"><span>⌕</span><input aria-label="Search datasets" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search datasets, organizations, tags..."/><kbd>⌘ K</kbd></div><div className="top-actions"><a className="icon-button" aria-label="About Dataset Atlas" href="https://github.com/shivam-raval96/safety-dataset-visualizer" target="_blank" rel="noreferrer">i</a><a className="primary" href="https://github.com/shivam-raval96/safety-dataset-visualizer/issues/new?title=Dataset%20suggestion" target="_blank" rel="noreferrer">Suggest a dataset <span>↗</span></a></div></header>
<section className="workspace"><aside className="filters"><div><p className="eyebrow">Explore</p><h1>The safety<br/>dataset space.</h1><p className="intro">A living map of the datasets shaping AI safety and alignment research.</p></div><nav aria-label="Dataset categories">{categories.map(c=><button key={c} onClick={()=>setFilter(c)} className={filter===c?'active':''}><span className="cat-dot" style={{background:c==='All datasets'?'#151718':colors[c]}}/>{c}<b>{c==='All datasets'?datasets.length:datasets.filter(d=>d.category===c).length}</b></button>)}</nav><div className="legend-note"><span>MiniLM + UMAP</span><p>Embedded from each dataset card, then projected by semantic similarity. Nearby datasets share themes and intended uses.</p></div></aside>
<section className="map" aria-label="UMAP visualization of AI safety datasets"><div className="map-head"><div><span className="live-dot"/> {visible.length} datasets visible</div><div className="map-tools"><button aria-label="Zoom in">＋</button><button aria-label="Zoom out">−</button><button aria-label="Reset view">⌖</button></div></div><div className="plot"><div className="orb orb-one"/><div className="orb orb-two"/><div className="orb orb-three"/>{visible.map((d,i)=><button key={d.name} aria-label={`${d.name}: ${d.desc}`} title={`${d.name} — ${d.desc}`} onClick={()=>setSelected(d)} className={`point ${selected.name===d.name?'selected':''}`} style={{left:`${d.x}%`,top:`${d.y}%`,background:colors[d.category],animationDelay:`${i*18}ms`,width:`${pointSize(d.size)}px`,height:`${pointSize(d.size)}px`}}><span className={visibleLabelNames.has(d.name)?'visible-label':''}>{d.name}<small>{d.size} samples</small></span></button>)}{visible.length===0&&<div className="empty">No datasets match that search.</div>}</div><div className="map-foot"><span>Proximity indicates similarity</span><span>Size indicates dataset size</span></div></section>
<aside className="detail" key={selected.name}><div className="detail-top"><div className="dataset-icon" style={{background:colors[selected.category]}}>{selected.name.slice(0,2).toUpperCase()}</div></div><p className="detail-category"><span style={{background:colors[selected.category]}}/>{selected.category}</p><h2>{selected.name}</h2><p className="org">by {selected.org} · {selected.year}</p><p className="description">{selected.desc}</p><div className="stats"><div><span>Samples</span><strong>{selected.size}</strong></div><div><span>Citations</span><strong>{selected.citations}</strong></div><div><span>License</span><strong>{selected.license}</strong></div></div><div className="detail-section"><p className="eyebrow">Research uses</p><div className="tags">{selected.tags.map(t=><span key={t}>#{t.replace(' ','_')}</span>)}</div></div><div className="detail-section related"><p className="eyebrow">Near in the atlas</p>{datasets.filter(d=>d.category===selected.category&&d.name!==selected.name).slice(0,3).map(d=><a key={d.name} href={d.url} target="_blank" rel="noreferrer"><i style={{background:colors[d.category]}}/><span>{d.name}<small>{d.org}</small></span><b>↗</b></a>)}</div><div className="source-actions"><a className="open-button" href={selected.url} target="_blank" rel="noreferrer">Open dataset source <span>↗</span></a>{selected.paper&&<a className="paper-button" href={selected.paper} target="_blank" rel="noreferrer">Read associated paper <span>↗</span></a>}</div></aside></section></main>}
